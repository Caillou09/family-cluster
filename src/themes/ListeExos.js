

import styled from 'styled-components'
import {pxToRem} from './helpers'

const ListeExos = styled.div`
  display : flex;
  flex-flow : row wrap;
  justify-content : space-evenly;
  width : auto;
`

export default ListeExos
